[tool.poetry]
name = "pssetools"
version = "0.4.0"
description = ""
authors = ["jbarberia <jbarberia@frba.utn.edu.ar>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^2.7 || >=3.7.1"
commentjson = "^0.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

# [tool.poetry.scripts]
# psse_estatico = "pssetools.scripts.estatico:estatico" 

